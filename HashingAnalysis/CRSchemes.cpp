#include "CRSchemes.hpp"
#include <iostream>
#include <list>

SepChain::SepChain(int size){
    std::list<int> hashMap[size];
}
int SepChain::insertKey(int keyValue, int address) {
    int numCol=0;
    
    return numCol;
}

SepChain::~SepChain() {
   
}


OpenAdd::OpenAdd(int size) {
    int* hashmap[size];
}
int OpenAdd::insertKey(int keyValue, int address) {
    int numCol=0;
    
    return numCol;
}

